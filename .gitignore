# --- Gradle / Kotlin ---
.gradle/
build/
**/build/
!gradle/wrapper/gradle-wrapper.jar
local.properties
.kotlin/

# --- Xcode / iOS ---
xcuserdata/
*.xcuserstate
DerivedData/
*.hmap
*.ipa
*.dSYM.zip
Pods/

# --- macOS ---
.DS_Store

# --- IDE ---
.idea/
!.idea/codeStyles/
*.iml

# --- Someone's Home ---
# Session recordings contain COMPLETE authority state — roles, real positions,
# real Egress progress. They are debugging artifacts, never committed.
recordings/
*.jsonl

# Local-only planning exhaust. Raw session transcripts, superseded by the
# GDD and decision log, which carry everything load-bearing from them.
_bmad-output/brainstorming/
PRIVATE-NOTES.local.md
*.local.md
