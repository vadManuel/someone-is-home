# Overnight — 2026-08-21

Unattended work against the frozen loop. Each unit builds up to the loop boundary and never
across it: no new Events, Effects, redaction-schema rows or `reduce()` changes. Anything that
touched what a client receives, what an ability reveals, the lamp, or a schema entry was written
into **Escalations** and skipped rather than guessed at.

Every guard below was proven by injecting the bug it exists for and watching it fail, with the
failure text quoted from the test-result XML. Counts are read from `tests=` / `failures=`, never
from an exit code.

---

## Unit 1 — the flow spine: a walkable screen graph in `:ui`

**Outcome: done, green, pushed.** Commit `4fdaaaa` — *"The screen list learns what may follow
what"*. Full `./gradlew check`: **tests=364,
failures=0** across every module and both targets (desktop and `iosSimulatorArm64`), up from 344
— 20 new. `DeviceLayoutTest` stayed green throughout (56 screens × 2 roles against a faked
notch), and `:ui:boundaryCheck` still reports `ui clean` — the flow layer sees no `:core`.

### What it is

`ScreenId` has always been *a screen list, not a navigation graph* — it says what exists and
nothing about what may follow what. `ui/src/commonMain/kotlin/home/someoneshome/ui/Flow.kt` is
the other half:

- **`ScreenGraph.exitsOf`** — what each screen's own controls reach, as an exhaustive `when` over
  `ScreenId`, so adding a screen without deciding what it reaches is a compile error rather than a
  screen with no way out. Same discipline as `Screen`'s own `when`.
- **`Flow.autoAdvance`** — the transitions the house makes with no tap, as a map. Boot's
  fall-through, the scan's ten-second window, the meeting from the ring to the lights going out,
  and the same meeting walked from outside the system.
- **`Flow.houseDriving`** — where stepping backwards is refused.
- **`Flow.housePushed` / `Flow.unrouted` / `Flow.viaActions`** — the three honest footnotes: the
  screens only the authority can reach, the two the port cannot reach at all, and the one edge the
  actions layer owns rather than a screen.
- **`FlowModel` + `FlowHost`** — the thing that walks it. `PanelState` stayed flat and inert; the
  trail sits beside it, is never read by a screen, and decides no game question.
- The playtest and debug roots now open on `Boot` and walk themselves. `CheatRoot`'s private
  local-state navigation is gone; the picker is now for the screens no walk reaches plus the role.

### The timings are placeholders, and that is the point

**Every number in `Flow.autoAdvance` is presentation timing.** In play the house owns the clock —
it decides when a ring is answered, when the talk starts, when the lights go out — and pushes the
screen. The table is what a phone with no house attached does instead, and it goes away when the
transport carries a round. Where the design already carried a number the table borrowed it rather
than inventing one; every row carries a `why` naming the cue on the screen that asked for it, and
a test fails a row that has none.

The one number that is *not* arbitrary is the scan window: `SCAN_SEGMENTS * 500` = ten seconds,
derived from the bar that draws it rather than restated beside it, so the countdown on screen and
the moment the light dies cannot drift apart.

### The transcription is read off the screens, not reviewed against them

`ScreenGraphTest` renders all 56 screens across both roles, both room types and markers on and
off, fires every tap target each one publishes via its click semantics action, and compares what
the screens asked for with what the graph claims. A retargeted button fails the build and names
both sides. It uses the semantics action rather than `performClick` deliberately: a pointer event
at a node's centre never reaches a control drawn underneath another one, so the graph would come
out *smaller* than the screen and agree with itself.

It passed on its first run against a graph transcribed from a throwaway probe of the same shape —
which is the result you want from a transcription, and worth nothing until it has been made to
fail (below).

### The no-back set is derived, not invented

D-102 ratified the haptic doctrine: *a screen that arrives unasked buzzes; a screen you tapped
into does not.* Read the other way round, a screen that buzzed is one the house put you on, and
nothing behind it is still true. So `houseDriving` is built from `PanelVals.BUZZING` minus the one
exception the doctrine itself names — the host's registration scan, which buzzes because the phone
is against a card with the display angled away — plus seven screens that buzz on nobody's phone
and are still the house's, each named with its reason at the declaration.

`PanelVals.buzzes` was a `setOf(...)` literal rebuilt on every construction of a `PanelVals`, i.e.
on every recomposition of every screen, to answer one membership question. It is now a
`companion object` constant. Against a whole-app budget of ~0.5 MB/s that is a small win on its
own; the reason it is here is that it makes the buzzing set a single source the flow layer can be
*derived* from rather than kept in step with by hand.

### What the tests actually hold

`FlowTest` (20, common — runs on desktop and on `iosSimulatorArm64`) proves the properties that
hold over the whole graph at once, which is exactly the kind nobody checks by walking a phone:

- every screen either offers a way onward or is one of six that are **waiting for the house**
  (revoked, restrained, outside the system, disconnected, and the two endings);
- starting from `Boot` plus the house-pushed screens, **every screen in the game is reached** — no
  orphans, nothing drawn and reachable by nothing;
- and its converse, that **nothing walks to a screen only the house can push**. That one bites: a
  tap wired straight to `Revoked` would let a client put itself out of the round, and the list
  would still say the house did it;
- back is refused on exactly the house's screens and works everywhere else, in both directions;
- the meeting is a line from any of the three ways it starts through to the lights going out;
- the two ways out are still not synonyms — walking onto an out screen carries the cause, so the
  carrier reads REVOKED or RESTRAINED rather than the UNREGISTERED fallback.

`FlowHostTest` (3, desktop) proves the *host* drives rather than the table being well-formed, on
the test clock so a ninety-second discussion is checked in microseconds.

### Injection log — five bugs written, watched to fail, reverse-replaced

Each was reverted with a targeted reverse-replace, never `git checkout`.

| injected | caught by | XML |
|---|---|---|
| `ScanBad`/`ScanUnknown`'s SCAN ANOTHER retargeted to `Home` | `ScreenGraphTest` | `tests="1" failures="1"` — *"ScanBad — the screen reaches [Home, Work], the graph says [Scan, Work]"* |
| `Tally`'s auto-advance row deleted, and `Tally` dropped from `houseDriving` | `FlowTest`, four independent tests | `tests="19" failures="4"` — *"unexpectedly stuck [Tally]"*, *"Tally does not move on; the meeting stalls there"*, *"Vote advances to Tally, which is a dead end"*, *"these buzz, so they arrived unasked, so back must be refused on them"* |
| `Tally` advanced into `Revoked`, and `Files` added to `houseDriving` | `FlowTest` | `tests="20" failures="5"` — *"Revoked is listed as unreachable by tapping, but something reaches it: [Tally]"*, *"the no-back set changed. Widening it strands a player on a screen they opened"* |
| `LaunchedEffect(Unit)` — the classic wrong key | `FlowHostTest` | `tests="3" failures="1"` — *"expected `[Call, Assemble, Notice, Discussion, Vote, Tally, Home]` but was `[Call, Assemble, Assemble, Assemble, Assemble, Assemble, Assemble]`"* |
| the same, plus the stale-screen re-check removed | `FlowHostTest` | `tests="3" failures="2"` — *"a spent scan window moved the phone anyway"* |

**The third injection changed a test.** `Files` in `houseDriving` was caught only by two tests
that happen to use `Files`, and not by the test whose subject is the no-back set — that one
derived its expectation from the very set it was checking, so a wrongly-*widened* set passed it.
`theHousesScreensAreExactlyTheOnesNamed` was added to close that, and the injection was left in
place until it failed by name. A guard that can only catch the set getting smaller is half a
guard.

### Decisions taken (minor, inside the established design language) — flagged, not silent

- **`RoomEdit`'s STAIRS chip now routes to `StairsWarn`, and the type does not change until the
  host answers.** Without it that screen is an orphan: nothing on any screen reaches it, because
  the chip calls `pickRoomType` rather than naming a target — the one place the port hands a
  navigation decision to the actions layer. The design's reason is D-099: stairs hold nothing, so
  turning an occupied room into one unregisters every card in it, and the host is told what that
  costs *before* it happens. Setting the type first and asking afterwards would be the destructive
  edit happening before the question about it. `Flow.viaActions` records it as an actions-layer
  edge rather than smuggling it into `ScreenGraph`, where it would be a claim about a control that
  does not exist. Confirming (UNREGISTER AND CONTINUE) sets the type and returns to the editor;
  the port's `confirmStairs` previously navigated without changing anything.
- **The port's held room always holds markers, so the warning is unconditional.** Whether a
  particular room holds anything is host-setup data that arrives with the state; an empty room
  should one day change type in place with nothing to warn about.
- **The trail is capped at 32.** A round is twenty-five minutes of tapping around a springboard,
  and a list that only ever grows is an allocation the whole-app budget pays for.
- **`arrivingAt` moved out of the cheats into `:ui`** and both callers now share it. A picker that
  landed on the revoked screen with no cause rendered UNREGISTERED and read as a bug in the chrome.

### Flagged for a person — not escalations, but somebody should look

- **`Ghost2` does not buzz.** A revoked player sitting in the dark is told to STAND AND WALK IN,
  on a near-black screen facing away from them, with no haptic. D-102's canonical set is twelve
  screens and `PanelVals.BUZZING` holds seventeen; `Ghost2` is in neither. A buzz is identical for
  every player so adding one is not a tell, but it is D-102's set and I did not touch it.
- **`TermTaken` and `NoTerminal` are unreachable — recorded in `Flow.unrouted`.** Both are
  host-setup refusals the host is meant to hit during the setup walk (a terminal card offered to a
  second room; a save attempted with no terminal anywhere), and both are stranded because the
  screen that ought to route to them names an unconditional target instead: `Editor`'s REVIEW HOME
  always goes to `SaveName` even when `saveLabel` reads NEEDS A TERMINAL, and `ScanMarker`'s DONE
  SCANNING always goes to `MarkerSheet`. Closing it is a change to those two screens, not to the
  flow layer, so it was left alone and written down.
- **`IntegrityWidget`'s KDoc is stale after D-103.** It still says *"28 of 32, thirty-two segments
  rather than a percentage"* while the widget it documents renders `88%`. The code is right and
  the comment describes the design that revision 21 superseded. Not touched — it is a one-line doc
  fix somebody should make deliberately rather than have an overnight unit make silently.
- **`PanelVals` builds its fixtures eagerly on every construction** — `inbox`, `ballots`,
  `roomMarkers`, `current` — which means once per recomposition of every screen. `buzzes` was
  fixed because the flow layer needed the set; the rest was left alone as out of unit. Worth a
  pass against the ~0.5 MB/s budget before there is a lamp on the same device.

### Simulator (layout and self-advance only — no BLE, torch, camera or haptics)

Playtest variant built and launched on the simulator. The app opened on `Boot` and **fell through
to `Perms` on its own**, then held there — the screenshot taken seconds later and the one taken
after are byte-identical, so nothing runs on past a screen that owes no advance. Whole screen
checked: status row level with the Island, nothing under the cutout or the home indicator, the
PLAYTEST chip where it has always been. No errors in the device log. Nothing here certifies
anything about the game.

---

## Unit 2 — the plan model: what the host drew, in `:model`

**Outcome: done, green, pushed.** Commit `34a7001` — *"The house learns its own shape"*. Full
`./gradlew check`: **tests=464, failures=0, errors=0** across every module and both targets, up
from 364 — 100 new (46 model tests running on both `jvm` and `iosSimulatorArm64`, 8 platform tests
on `iosSimulatorArm64`). Both lints and `:ui:boundaryCheck` still green; nothing in this unit goes
near `:ui`.

### What it is

The setup walk has two halves. `HouseMap` was the half that binds cards to rooms; `HousePlan` is
the half that says what the rooms **are**.

- `model/.../HousePlan.kt` — `Cell`, `CellRect`, `PlanRoom`, `Floor`, `HousePlan`, `PaintResult`.
  One grid plan per storey, rooms painted as unions of rectangular drags, named and tagged room or
  stairs. `paint` refuses politely so an editor can say why with the finger still on the screen;
  `HousePlan.of` `require`s the same things as the last-ditch guarantee for storage and every other
  path. Same shape as `HouseMap.register` and `Registration`'s `init` (D-099).
- `model/.../HousePlanText.kt` — format version 1, `someone-is-home/house-plan/1`. HouseMapText's
  doctrine applied line for line: versioned header checked rather than tolerated, every failure
  throwing `MalformedHousePlan` naming the line, nothing skipped, names escaped so a room called
  `KITCHEN|room|0,0,1,1` cannot forge a row.
- `platform/.../HousePlanStore.kt` + the iOS actual, beside `HouseMapStore`.
- `platform/.../DocumentStore.ios.kt` — the atomic write, the Documents path and the
  create-if-absent step, now written once instead of twice. `HouseMapStore.ios.kt` is rewired onto
  it and keeps the bug story that produced it in the shared file's KDoc.

### A room is a LIST of rects, not one rect

**This is a deliberate departure from the unit brief's wording, and it is the whole reason the
grid exists.** The GDD argues the grid over free rectangles on three counts, and the second is
that *L-shaped rooms just work, because real houses have them and rectangles cannot express them*.
A room modelled as a single rect deletes that quietly: an L-shaped kitchen becomes two rooms with
two names, and a player told to go to the kitchen has two places to stand — which is exactly the
`ShapeAlreadyRegistered` hazard D-086 refused, arriving through geometry instead. The **stroke**
is rectangular, because a drag is; the **room** is the union. E4's acceptance criterion —
*an L-shaped room is expressible* — has a test on it that reads the eight cells back.

The other two counts are paid in the same file. `Floor.neighboursOf` derives adjacency from cell
neighbours with no geometry (E4.9's *adjacency is queryable without geometry*), and there are no
resize handles, overlap rules or snapping anywhere in it.

### Decided here, and flagged rather than buried

1. **Adjacency is four-neighbourhood, not eight.** Two rooms meeting at one corner share no
   doorway; a diagonal neighbour would report a route through a wall, and error injection would
   place a player somewhere they could not have walked to. Injection-verified — adding the two
   diagonals fails `rooms touching only at a corner are not neighbours`.
2. **Adjacency never crosses a floor, stairs included.** *No vertical-connection logic* — the app
   renders what was drawn. A property test holds it.
3. **`Floor` carries no number.** Floors are purely additive, and an index is an ordering, which
   is one short step from "the stairs on floor 0 lead to floor 1" — the logic the design refused.
   Position in `floors` is the only order there is and means only the order they were added in.
4. **Room names are unique across the whole plan, not per floor.** A card is registered to a
   `Room`, which is a name and a kind — so two rooms called BATHROOM on different storeys give a
   registration two places it could mean, and the host who painted them is the only person who
   would ever know. Costs a host one rename during setup, in the light. **This is a judgement
   call and is the one most worth a second opinion.**
5. **`HousePlan.LIMIT = 256` cells.** A format sanity bound, **not a balance value** — a file
   claiming a stroke two million cells wide is corrupted, and expanding it to cells before finding
   that out is how a host's phone dies mid-setup. Roughly six times the widest plan anyone would
   paint by hand.
6. **Negative coordinates are ordinary.** A host who starts painting in the middle of the hall and
   works outwards must not hit an origin.
7. **A separate file, not a section of the map file.** The plan and the map are lost differently —
   a host repaints a wall without re-registering forty cards. One file means every save rewrites
   both, so a plan could be destroyed by a save that was only ever about a card. A test holds it.
8. **No `housePlanStorageDirectory()`.** There is one setup directory and `houseMapStorageDirectory`
   already names it.

### The bug the injection pass found

The reader tracked the open storey's name in a `var` starting at `""`. That sentinel is **also**
what a nameless floor row parses to, so `F ` was refused as *"two floors called ''"* by the
duplicate check before the empty-name check could speak. The right answer for the wrong reason,
and the reason is the half a host reads. It surfaced only because the empty-name guard came back
ASLEEP under injection — no test failed when it was disabled, because another guard was quietly
answering for it. The name and its rooms are now one nullable pair, and a test asserts the
message rather than only the refusal.

### 31 guards, each proven by breaking it

Every refusal was injection-verified: break the guard, watch the named test fail, read the failure
from `model/build/test-results/jvmTest/TEST-*.xml`, put it back by **targeted reverse-replace**
with a byte-identical assertion on the restored file. Never `git checkout` — the files were
uncommitted the entire time. Sixteen reader refusals (header, unknown row, room-before-floor,
field count, kind token, four stroke-parse cases, the grid limit, empty names, duplicate floor,
duplicate room, overlap, and the two escape cases), eleven construction `require`s, and four
editor refusals plus the adjacency shape.

A sample, quoted from the XML:

- `two floors with one name are refused on the second one's line` — *Expected an exception of class
  MalformedHousePlan to be thrown, but was java.lang.IllegalArgumentException: two floors called
  'GROUND'* (the reader stopped naming a line and let `HousePlan.of` answer instead)
- `a stroke past the grid limit is refused before it becomes cells` — *…but was
  java.lang.IllegalArgumentException: a painted stroke of 2000000x1 is past the 256-cell limit*
- `rooms touching only at a corner are not neighbours` — *expected:`[]` but was:`[PlanRoom(room=Room(name=PORCH, kind=Room), …)]`*
- `an unknown row is refused rather than skipped` — *…but was completed successfully with the
  result: `<HousePlan@117…>`* (a plan one room short, and nobody would know)

One test in the file is **not** a guard and is labelled as such: `adjacency never crosses a floor`
is a property of the structure — there is no cross-floor path to disable — so it was not injected.

### Not in this unit

Markers into cells (4.4), the Terminal and Array Wipe designations (4.5), the staircase
acknowledgment screen (4.8), the couch dead zone (4.10), the QR PDF (4.11), and the painter UI
itself. The loop is untouched: no Events, Effects, redaction-schema rows or `reduce()` changes.
Nothing here is read during a round.

**One follow-on worth someone's time, not raised as an escalation because it is a balance number
and those are out of scope:** decision-log:219 wants the Egress timer's *default* proposed from
the painted grid rather than fixed at 120s. The geometry that makes that computable now exists.

---

## Unit 3 — the editor, real: `HomeEditorModel` in `:ui` over U2's plan

**Outcome: done, green, pushed.** Commit `909f4cf` — *"Two corners and a name"*. Full
`./gradlew check`: **tests=525, failures=0, errors=0** across every module and both targets, up
from 464 — 61 new (26 editor tests running on both desktop and `iosSimulatorArm64`, 5 gesture
tests on desktop, 2 flow tests on both). Both lints green, `:ui:boundaryCheck` still reports
`ui clean`, `DeviceLayoutTest` green throughout.

### What it is

The host-setup screens were a picture of one bungalow: six rooms in a hardcoded rect list, six
label placements nudged by hand, "6 ROOMS . 5 MARKERS" written down, and a plan you could tap
anywhere on to reach a room panel that always said GARAGE. They are now an editor.

- `ui/HomeEditor.kt` — `HomeEditorModel`, the editing state, sitting **beside** `PanelState` the
  way `FlowModel`'s trail does. `PanelState` stayed flat and inert; a plan under a finger is
  neither flat nor decided. Nothing in it answers a game question and nothing can — it is
  host-side setup drawn in the light, saved, and never read during a round, which is what makes
  real mutable state lawful in a module that cannot see `:core`.
- `EditorSurface` — the grid under the finger. Drag two corners on bare grid to paint a room;
  **drag starting inside a room to add a stroke to it**, which is how an L-shaped room gets
  drawn. `FloorTab`s switch storeys, the labels and chips are read off the painted rooms, and
  the drag draws a live preview that goes to the caution colour when it would be refused.
- Every refusal `HousePlan.paint` can return is translated in one place, so a crossing drag says
  THAT CROSSES KITCHEN under the host's finger rather than doing nothing — which is
  indistinguishable from a missed touch.
- The room panel's five preset chips and the name field are the same call. A rename carries the
  room's cards and the T card with it.
- Floors are real: tabs switch, ADD A FLOOR adds one and opens it empty, and every count on
  Floors and Save Home is counted off the plan.
- REVIEW HOME asks the plan whether any room holds the T card, and goes to `NoTerminal` when
  none does.

### D-098 and D-099 surfaced rather than re-derived

D-098 already held structurally — `RoomKind` has carried exactly `Room` and `Stairs` since
revision 20 — so the type chips read that enum directly and `ui.RoomType`, which was a second
copy of it, is deleted. D-099 is enforced *in the change*: `setKind(Stairs)` unregisters the
room's cards as part of becoming stairs, not in a step that follows it. `PanelState.roomType`
and `PanelState.hasTerminal` went with it: the plan is where a room's type and the terminal are
written down, and a second copy on `PanelState` is a second copy that would one day disagree
with the plan it describes.

### The stairs warning is conditional now, which closes a unit-1 flag

Unit 1 recorded: *"The port's held room always holds markers, so the warning is unconditional…
an empty room should one day change type in place with nothing to warn about."* The plan knows
what a room holds, so it does. A host tagging a bare stairwell is no longer interrogated about
cards that were never there.

### `NoTerminal` is routed, which closes another

`Flow.unrouted` is down to `TermTaken` alone. `TermTaken` stays stranded for the same reason it
always was, now written down properly: it is the refusal for *scanning a second T card*, which
is story 4.5 and has no camera behind it yet.

### Two bugs the gesture test found, and nothing else could have

`EditorSurfaceTest` drives real touch input. It exists because `ScreenGraphTest` fires click
**semantics actions** and a `pointerInput` publishes none, and `DeviceLayoutTest` measures
pixels — so between them the entire painting gesture could have been wired to nothing with every
test green. Both of these were live in code that had already passed a full green `check`:

1. **`detectDragGestures` reports the position at which a gesture *became* a drag**, after touch
   slop. The anchor arrived a cell late: a four-cell room came out three cells, and a drag
   started inside a room made a *new* room beside it instead of growing it —
   `expected:<[Cell(x=4, y=0), Cell(x=4, y=1), Cell(x=4, y=2), Cell(x=4, y=3)]> but
   was:<[Cell(x=4, y=1), Cell(x=4, y=2), Cell(x=4, y=3)]>`. Rewritten with `awaitFirstDown`.
2. **`awaitFirstDown` defaults to `requireUnconsumed`**, and the tap detector is the inner node
   and consumes the down first. The drag then waits forever and paints nothing, silently — *"the
   drag painted nothing: null"*.

The model's own tests could not have caught either: they are *told* which cells the finger
crossed, so they were being told the truth about a lie.

### Injection log — 22 guards, each broken, watched to fail by name, reverse-replaced

Never `git checkout`; the files were uncommitted the whole time. Read from
`ui/build/test-results/desktopTest/TEST-*.xml`, never an exit code.

| injected | caught by | failure text |
|---|---|---|
| `spanning` takes the corners in order rather than their extents | `HomeEditorTest` ×4 | *a painted stroke covers no cells (-1x1) — a drag that selected nothing* |
| `dragBlocked` does not exempt the room being grown; `dropDrag` always makes a new room | `HomeEditorTest` ×2 | *the kitchen was shown as blocking itself*; *the second drag made a new room instead of growing the first* |
| the overlap refusal is swallowed; the rename drops markers and the terminal; the duplicate-name check removed | `HomeEditorTest` ×5 | *expected:`<THAT CROSSES KITCHEN>` but was:`<null>`*; *expected:`<A ROOM IS ALREADY CALLED KITCHEN>` but was:`<null>`* — the rename **succeeded**, replacing the kitchen's cells with the garage's |
| stairs no longer unregister; `unregisterAll` leaves the terminal; `deleteHeld` holds nothing | `HomeEditorTest` ×5, `FlowTest` ×1 | *the terminal survived its room becoming a walk-through*; *the panel is left holding nothing while rooms remain*; *stairs hold nothing* |
| `addFloor` does not open; `openFloor` does not re-hold; the sample's rect height off by one; a tap on bare grid opens the panel | `HomeEditorTest` ×3, `FlowTest` ×1 | *expected:`<FLOOR 3>` but was:`<GROUND>`*; *the panel is still holding a room on the storey the host left*; *an empty cell opened the room panel* |
| REVIEW HOME goes to `SaveName` unconditionally | `ScreenGraphTest` | *Editor — the screen reaches `[Maps, Floors, SaveName]`, the graph says `[Maps, Floors, SaveName, NoTerminal]`* |
| the stairs warning made unconditional, then removed entirely | `FlowTest` ×1, then ×2 | *nothing was at stake and it asked anyway expected:`<RoomEdit>` but was:`<StairsWarn>`*; then *expected:`<StairsWarn>` but was:`<RoomEdit>`* |
| provisional room/floor names not checked for freedom; `markerCount` counts rooms not cards; a drag paints onto a fixed storey; the empty-name check disabled | `HomeEditorTest` ×6 | *expected:`<ROOM 1>` but was:`<ROOM 7>`*; *a floor called 'FLOOR 3' is already in this house*; *the ground floor grew a room expected:`<6>` but was:`<7>`*; *a room with no name* |

### Decisions taken (minor, inside the established design language) — flagged, not silent

- **A drag that starts inside a room grows that room.** The design says only *drag two corners to
  add a room*, but a room is a list of strokes and the whole point of the grid is that L-shaped
  rooms work — an editor that could only ever make single-rect rooms deletes E4's acceptance
  criterion quietly. Starting inside a room is unambiguous: a drag from bare grid can only be a
  new room, and a second room where one already is would overlap. The editor's hint gained a
  third line, DRAG FROM INSIDE ONE TO GROW IT, or it is undiscoverable.
- **A tap on the plan is an actions-layer edge** (`Flow.viaActions[Editor] = {RoomEdit}`), and
  `ScreenGraph.exitsOf(Editor)` no longer claims `RoomEdit`. Where a tap lands depends on which
  room is under it, and on most of a grid there is no room and it lands nowhere — a screen cannot
  name a target that is a position. Walked by a test, like the other two.
- **`NoTerminal`'s OPEN A ROOM goes to `Editor`, not `RoomEdit`.** The host has to choose *which*
  room the terminal goes in — *put it somewhere awkward* is the load-bearing line — and only the
  plan can ask that.
- **The editor draws no held room; the room panel does.** The panel always holds a room so it
  never opens on nothing, but an editor that lit the held one would open on a house with a room
  already picked out, reading as a selection the host did not make. Confirmed by screenshot: the
  first render had KITCHEN dark for no reason a host could explain.
- **New rooms are named `ROOM 1`, `ROOM 2`…, new storeys `FLOOR 3`, `FLOOR 4`…** A painted room
  must have a name, unique across the plan, because that is what a card is registered to.
  Obviously-provisional is what sends the host to the panel to say what it really is.
- **Room labels sit at each room's top-left cell.** The port's hand-tuned placements described one
  bungalow; a host who painted their own house got six labels in the wrong places.
- **The T chip is drawn as well as the count, not instead of it.** A room can hold cards *and* the
  terminal, and a chip that showed one would hide the other from the host about to go and find it.
- **DELETE ROOM names what it takes**, pluralised, and says nothing about cards when there are
  none. **The editor's viewport is a fixed ten-by-twelve window** — the design's own grid — over
  an unbounded coordinate space; panning is a later story.
- **Markers and the terminal are fixtures held beside the plan** (`markers: Map<String, List<
  MarkerShape>>`, `terminal: String?`) — 4.4 and 4.5 build the real thing. What is real now is
  the *shape* of that data, which is what lets D-099 and the REVIEW HOME gate be real rules
  rather than flags.
- **`ui.PlanRoom` renamed `PlanRect`.** It is a rectangle on the live-map fixture;
  `model.PlanRoom` is the painted room. Two types under one name in one package, introduced by
  this unit, was going to cost somebody an hour.

### Flagged for a person — not escalations, but somebody should look

- **All five preset chips are already taken in the sample home.** KITCHEN, LIVING, GARAGE, BATH 1
  and BED 1 are all painted in the bungalow, so in that plan every chip refuses. Correct
  behaviour and useless demonstration; a host starting from an empty plan sees them work. Worth
  deciding whether the chips should grey out when the name is taken.
- **Renaming and deleting a *storey* are still inert.** Both need something this unit does not
  build — a text field for one, a two-second hold for the other. They name the open storey
  truthfully and do nothing, which is better than naming a storey that is not open and doing
  nothing, but they are two dead controls.
- **The floor tabs will crowd past four storeys.** They are weighted in a fixed-height row, as
  the design drew them for two. `FLOOR 3` at chip size in a quarter of 300 units will clip.
- **The home's own name is still the fixture's THE BUNGALOW** on the editor heading, and Saved
  Homes / Home Detail / Delete are still fixtures. Naming a home is the Save Home screen's field
  and wiring saved homes is the persistence story, both out of this unit.
- **`PanelState.markersOn` and the whole `marker*` block of `PanelVals` are dead** — no screen
  reads any of them. They belong to the design's marker-placement mode (4.4). Left alone as out
  of unit, but they are currently an axis `ScreenGraphTest` spends renders on for nothing.
- **`IntegrityWidget`'s KDoc is still stale after D-103** — unit 1 flagged it, still true.

### Simulator (layout licence only — no BLE, torch, camera or haptics)

Playtest variant built and launched twice: once as it ships, where the app opened on `Boot` and
fell through to `Perms` on its own, and once with the playtest root temporarily started on
`Editor` so the grid could be looked at on a phone. Reverted by targeted reverse-replace. Whole
screen checked on both: status row level with the Island, nothing under the cutout or the home
indicator, the PLAYTEST chip where it has always been, room labels and marker chips landing on
their rooms, the stairs hatch reading as transit. Nothing here certifies anything about the game.

---

## Unit 4 — saved homes, wired to the phone's own filesystem

**Outcome: done, green, pushed.** Commit `eb3b958` — *"The house survives the phone being killed"*.
Full `./gradlew check`: **tests=639, failures=0, errors=0** across every module and both targets.

**On the count.** 639 is the number this tree produces from `tests=`/`failures=` across every
`*/build/test-results/**/TEST-*.xml` after deleting the results directory and re-running. The same
count run against `HEAD` in a throwaway `git worktree` gives **509**, so this unit adds 130. Unit
3's section above reports 525 for the same commit, which means it counted something this method
does not (probably the nested variant sweeps, whose results overwrite `app`'s directory in place).
Recorded rather than reconciled: two different numbers for one commit is worth somebody knowing
about before it is quoted again.

### What it is

Saved homes were three hardcoded rows going to a detail screen about a fixture. They are now the
host's own, on the phone, surviving the app being killed.

- `model/.../SavedHome.kt` — a named home: the plan, what is registered in which room, and the one
  room holding the terminal. Refuses a home with no name, cards in a room the plan does not have,
  and **cards or a terminal in stairs** — D-099's last-ditch guarantee at the storage boundary,
  the same shape `Registration`'s `init` already is.
- `model/.../SavedHomesText.kt` — format version 1, `someone-is-home/saved-homes/1`. Same doctrine
  as `HouseMapText` and `HousePlanText`: versioned header checked rather than tolerated, every
  failure throwing `MalformedSavedHomes` naming the line, nothing skipped.
- `platform/.../SavedHomesStore.kt` + the iOS actual — `saved-homes.txt` beside the map and the
  plan, atomic, and a failed save throws (D-087).
- `ui/.../SavedHomes.kt` — `SavedHomesModel` beside `PanelState` the way the editor is, over a
  `HomeStore` port of two functions, because `ui` cannot see `platform`.
- `app/.../SavedHomesDocument.kt` — the port's real implementation. This is what `app` is for.
- `HomeEditorModel` now holds a whole home rather than only its geometry: `name`, `load`,
  `startNewHome`, `asSavedHome`.

### The plan's rows are the plan's, written by the plan's own writer

A home's plan sits in the middle of this file as `HousePlanText` wrote it, header and all, and is
read back by `HousePlanText`. There is no second copy of the plan grammar here, and the two
version numbers keep answering their own questions.

**A refusal from inside a plan is re-thrown against the line it really occupies in this file.**
`HousePlanText` counts from the top of what it is handed, which here is the middle of a longer
file; a host sent to line 2 for a row that is on line 5 goes looking at the wrong house. The row's
real line number is carried alongside it and swapped in.

Escaping is backslash and newline only, and that is a decision rather than an omission: **no row
here ends in a field that could be confused for another.** `H` and `T` take the rest of the line
and `M` puts the shapes first — lowercase words by construction — and splits at the first pipe. A
room called `KITCHEN|ring|LIVING` survives intact, and there is a test on exactly that. It also
means this file is not a third copy of the pipe-escaping logic the other two formats share.

### The relaunch proof, on the simulator, in two builds

The write and the read were deliberately done by **different builds**, because a process that
wrote a file and then read its own memory back has proved nothing.

1. **Build A** carried a temporary launch-time effect calling the real `FlowModel.saveHome()`.
   Screenshot: `HomeDetail` showing `THE ANNEXE / 2 FLOORS . 11 ROOMS . 9 MARKERS / ABOUT FIFTEEN
   MINUTES OF WALKING`.
2. **The container was read, not the code** — the practice D-096 named. `xcrun simctl
   get_app_container … data` → `Documents/saved-homes.txt`, 502 bytes, holding `H THE ANNEXE`, the
   eleven `R` rows across two `F` rows, eight `M` rows and `T HALL`.
3. **Build B had the writer removed** and the playtest root temporarily started on `Maps`.
   Screenshot: `SAVED HOMES` listing `THE ANNEXE — 2FL . 11RM`, read off the phone by a build with
   no code in it that could have written it.
4. **Killed and relaunched** — `simctl terminate`, confirmed gone from `launchctl list`, launched
   again: the screenshot is **byte-identical** to the one before it.

Both temporary edits were reverted by targeted reverse-replace, never `git checkout`, and the
simulator's `saved-homes.txt` was removed afterwards so the next unit's screenshots do not inherit
a house from this one.

The unit test `a second model over the same store sees the same homes` is the same proof in
miniature and is the one that fails on the build.

### Injection log — 15 guards, each broken, watched to fail by name, reverse-replaced

Read from the test XML, never an exit code. Never `git checkout`; the files were uncommitted
throughout.

| injected | caught by | failure text |
|---|---|---|
| the stairs check inverted in `SavedHome` | `SavedHomesTest` ×2 (+6 collateral) | *Expected an exception … but was completed successfully*; and *'THE BUNGALOW' says cards are registered in 'KITCHEN', which is stairs* |
| an unknown row skipped rather than refused | `SavedHomesTest` | *Expected an exception of class MalformedSavedHomes … but was completed successfully* |
| the duplicate-home-name check disabled | `SavedHomesTest` | *'X' has no plan* — the right refusal for the wrong reason, caught because the test asserts the line **and** the detail |
| the plan's refusal reported at its own line | `SavedHomesTest` | *expected:`<5>` but was:`<3>`* |
| an unknown shape id falling back to a default | `SavedHomesTest` | *Expected an exception … but was completed successfully* |
| newline not escaped in a name | `SavedHomesTest` | *unknown row 'UPSTAIRS 🕯'* — the forged row, exactly |
| the duplicate-card-row and two-terminal checks removed | `SavedHomesTest` ×2 | *…but was completed successfully* |
| the store's filename changed to the plan's | `SavedHomesStoreTest` (simulator) | *keeping a home destroyed the painted plan* |
| the list updated before the write instead of after | `SavedHomesModelTest` | *the list showed a home the phone does not have* |
| `unreadable` no longer blocking writes; the name-clash check removed; the open home no longer replaced | `SavedHomesModelTest` ×4, `FlowTest` ×2, `HostSetupInputTest` ×1 | *expected:`<[MUM & DAD'S]>` but was:`<[MUM & DAD'S, THE BUNGALOW]>`*; *a rename left a second copy behind*; *a refused save walked away anyway* |
| the hold confirming on touch rather than after two seconds | `HostSetupInputTest` ×2 | *the home went the moment a finger landed on it expected:`<3>` but was:`<2>`* |
| the hold carrying its previous attempt over | `HostSetupInputTest` | *hold 2 finished off the one before it expected:`<3>` but was:`<2>`* |
| the field ignoring what was typed | `HostSetupInputTest` ×3 | *the field did not shout it expected:`<THE [ANNEXE]>` but was:`<THE [BUNGALOW]>`* |
| `mapNewHome` not emptying the editor; `openSavedHome` loading it | `FlowTest` ×2 | *expected:`<0>` but was:`<11>`*; *the half-painted house was replaced by a tap* |
| HOLD TO DELETE wired as an ordinary tap | `ScreenGraphTest` | *Delete — the screen reaches `[HomeDetail, Maps]`, the graph says `[HomeDetail]`* |

**Two injections changed a test rather than only proving one.** The hold tests originally failed
with *"Failed to inject touch input … could not find any node"* — true, but it names the lift
rather than the fault, and it would send the next person looking at the wrong control. Both now
assert **with the finger still down**, and fail by name.

### Decisions taken (minor, inside the established design language) — flagged, not silent

- **PLAYED 3 TIMES and LAST PLAYED 12 AUGUST are gone from Home Detail**, replaced by `ABOUT
  FIFTEEN MINUTES OF WALKING`. Nothing in this app has ever hosted a round, so a play count is a
  number with no source that a host would nonetheless believe. The line that replaced it is one
  the plan really knows.
- **The minutes are derived: a room is a minute, a card is half of one, rounded to the nearest
  five, never below five.** Against the design's own home — eleven rooms, nine markers — that is
  *fifteen*, which is the number the design wrote. It is presentation, not a balance value:
  nothing locks at arming and nothing reads it.
- **DUPLICATE was inert and now works** — a copy under a free name, opened, staying on the screen.
  The unit brief lists three rows for this screen and this is a fourth; it was already drawn, and
  a working control beats both a dead one and a deleted one. **Say if it should go.** No export
  was added anywhere.
- **The name field is the first real text input in this interface.** A `BasicTextField` in the
  readout face, with the field's own caret instead of the drawn one, so there is exactly one caret
  and it sits where the next character goes. Typing is uppercased on the way in, like a room name.
- **MAP A NEW HOME empties the editor**; opening a home from the list does not. Opening is
  looking; EDIT THE PLAN and RENAME are the two controls that say they will replace what you were
  painting, and they are the two that do.
- **A new home is offered `HOME 1`**, obviously provisional, for the editor's `ROOM 2` reasons.
  A copy of `THE BUNGALOW 2` is `THE BUNGALOW 3`, not `THE BUNGALOW 2 2`.
- **The list is most-recently-saved first**, and the top row is the brighter of the design's two
  intensities. That is the only ordering claim the screen makes.
- **A file this build cannot read is never written over.** The model comes up empty and
  `unreadable`, every write is refused while that holds, and Maps says so where the homes should
  have been. Coming up empty and saving on top would delete every house on the phone to make room
  for one, silently.
- **A failed write is a refusal on screen, not a crash.** D-087 says a failed save throws so that
  success is never reported; the throw still happens in `platform`, and `SavedHomesModel` catches
  it, leaves the list exactly as it was, and says THIS PHONE DID NOT SAVE IT. Louder to the host
  than a crash, and it does not take the plan with it.
- **Empty state**: NO HOMES YET. MAP ONE AND IT STAYS ON THIS PHONE. The design never drew one.
- **The lobby now names the home the host chose**, because HOST WITH THIS HOME otherwise walked
  them to a screen naming a different house. Everything else on that screen is still a fixture.
- **`Screen` gained a `homes` parameter** defaulting to the design's three with the bungalow open —
  the state every host-setup screen was drawn in, and what keeps `ScreenGraphTest` and
  `DeviceLayoutTest` rendering the screens the design intended.

### Flagged for a person — not escalations, but somebody should look

- **`HousePlanStore` (unit 2) now has no caller.** A home's plan is stored inside `saved-homes.txt`
  and nothing reads or writes `house-plan.txt`. It was left alone rather than deleted — removing
  another unit's shipped API overnight is not this unit's call — but two stores for the same data
  is the drift hazard this project keeps writing down. Delete it, or say what it is for.
- **A newly painted home still cannot be saved**, because REVIEW HOME is gated on a terminal and
  nothing places one without a camera (story 4.5). That is why the relaunch proof saved the
  editor's fixture rather than a hand-painted house. It is correct behaviour and it means the
  MAP A NEW HOME path currently dead-ends at NEEDS A TERMINAL on a phone.
- **Deleting a home cannot be proved on the simulator**, because there is no way to hold a finger
  down through `simctl`. It is proved at the store level instead (`deleting the open home takes it
  off the phone` re-reads the store), and by the gesture test against the real touch stack.
- **`Delete` and `SaveName` moved from `ScreenGraph` into `Flow.viaActions`.** Both are honest —
  a hold publishes no click action, and a refused save stays put — but it is two more edges the
  rendering test cannot read off a screen, and `viaActions` is now five entries rather than three.
- **`IntegrityWidget`'s KDoc is still stale after D-103** — units 1 and 3 flagged it; still true.
- **`PanelState.markersOn` and the `marker*` block of `PanelVals` are still dead** — unit 3
  flagged it; still true.

### Simulator (layout licence only — no BLE, torch, camera or haptics)

Whole screen checked on both screenshots: status row level with the Island, nothing under the
cutout or the home indicator, the PLAYTEST chip in its usual place over the bottom control. The
detail screen's middle is empty by design — the delete button is pushed to the bottom — which is
worth a look by somebody with an opinion about it. Nothing here certifies anything about the game.

---

## Unit 5 — marker registration: a card, a room, and every answer the map can give

**Outcome: done, green, pushed.** Commit `083f0f6` — *"The cards stop being a picture of
cards"*. Full `./gradlew check`: **tests=724, failures=0, errors=0** across every module and both
targets. The same count run against `HEAD` (`eb3b958`) in a throwaway `git worktree` gives **639**,
so this unit adds **85**. Both lints green, `:ui:boundaryCheck` still reports `ui clean`,
`DeviceLayoutTest` green throughout.

### What it is

The setup walk's second half. Unit 2 gave the host a plan, unit 3 an editor, unit 4 somewhere to
keep it — and the cards in it were `Map<String, List<MarkerShape>>`, a fixture with every rule
already built on top of it. The rules were real; the data underneath them was not.

- `platform/.../CardScanner.kt` — the camera contract and the deck that stands in for it. It hands
  over **the payload**, nine characters as printed, not a decoded card: decoding is `CardPayload`'s
  in `model`, and a scanner that decided what a card *is* would be a second opinion about a format
  that describes paper. `SeededCardScanner` encodes real `MarkerCard`s with `CardPayload.encode`,
  so what the flow receives is what a camera would have produced and the decoder is exercised
  rather than bypassed.
- `HouseMap` learned the terminal. `register` routes the card marked T to `terminal` and never into
  `registrations`; `moveTerminal`, `forgetTerminal`, `forgetIn` and `renamedRoom` are the rest of
  what an editor needs from it. One new result, `TerminalTaken`.
- `HouseMapText` is **version 2** — it carries a `T` row. `SavedHomesText` is **version 2** and now
  embeds `HouseMapText.write(map)` beside `HousePlanText.write(plan)`, headers and all, the way
  unit 4 embedded the plan. Its own `M`/`T` rows are gone; there is no second copy of either
  grammar in it.
- `SavedHome` is `(name, plan, map)`. `HomeEditorModel` holds a real `HouseMap` and a `ScanOutcome`.
- `FlowModel.cardScanned(payload)` is where a card enters. `ScanMarker` shows what happened;
  `TermTaken`, `TermRemove` and the marker sheet's chips do what they say.
- The cheat surfaces gained a **shutter**: a chip that appears only on `ScanMarker`, names the card
  it will present, and calls the fake. Compiled out of release with the rest of that file.

### Both embedded formats spell a row `R`, and they do not mean the same thing

A painted room in one, a registered card in the other. The reader assigns a row to whichever
section is open and a section opens at its own header, which is why the headers are written out
rather than stripped. **A row arriving before either header is refused rather than guessed at** —
guessing files a painted room as a registered card or the other way round, and the file would read
back clean.

### Version 1 saved homes are refused by name, not migrated

A v1 file holds shapes with no printed ids. There is nothing honest to turn one into: an invented
id is a card the host does not have, and the card it collides with is a real piece of paper. The
refusal says what changed rather than reporting an unexpected string. **This deletes any home
saved by an earlier build** — which today is nothing outside a simulator container, but it is the
kind of thing worth knowing was decided rather than overlooked.

### Injection log — 23 guards broken, watched to fail by name, reverse-replaced

Never `git checkout`; the files were uncommitted throughout. Read from the test XML, never an exit
code. A sample:

| injected | caught by | failure text |
|---|---|---|
| `register` no longer routes the T card | `TerminalTest` ×6 | *expected:`<Room(name=HALL, kind=Room)>` but was:`<null>`*; and the reader's own *'1+TTTTTTT' is the card marked T, registered as an ordinary marker — it never is* |
| the terminal moves silently instead of asking | `TerminalTest`, `FlowTest`, `RegistrationTest` ×3, `HostSetupInputTest` | *expected:`<TermTaken>` but was:`<ScanMarker>`*; *the host was not told which room lost it expected:`<HALL>` but was:`<KITCHEN>`* |
| `forgetIn` leaves the terminal behind | `TerminalTest`, `HomeEditorTest` ×2, `ScreenGraphTest` | *the terminal survived its room becoming a walk-through* |
| `renamedRoom` drops the terminal | `TerminalTest`, `HomeEditorTest` | *expected:`<LANDING 2>` but was:`<HALL>`* |
| `HouseMap.of`'s T-card `require` removed | `SavedHomesTest` | *Expected an exception … but was completed successfully with the result: `<HouseMap@41f69e84>`* |
| the `T` row is not written | `SavedHomesTest` ×5, `TerminalTest`, `FlowTest`, `RegistrationTest`, `SavedHomesModelTest` ×3 | *the terminal stayed behind expected:`<HALL>` but was:`<null>`* |
| the map section header is ignored, so card rows land in the plan | `SavedHomesTest` ×17, `FlowTest` ×6, `SavedHomesModelTest` ×5, `ScreenGraphTest`, … | *a room row is kind\|cells\|name, and this one has 2 field(s)* |
| a map refusal reported at its own line rather than the file's | `SavedHomesTest` ×5 | *expected:`<7>` but was:`<2>`* |
| the plan/map agreement check dropped | `SavedHomesTest` ×3 | *Expected `MalformedSavedHomes` … but was `IllegalArgumentException: 'X' says cards are registered in 'CELLAR'* — the right refusal for the wrong reason, so the host is sent to no line at all |
| `ShapeAlreadyRegistered` produces no readout | `RegistrationTest` | *Expected `ScanOutcome$Refused`, actual `<null>`* |
| an unreadable payload produces no readout | `RegistrationTest` | *Expected `ScanOutcome$Unreadable`, actual `<null>`* |
| the marker chip's tap removed | `HostSetupInputTest` | *the wrong card came off the sheet expected:`<[HOME-05]>` but was:`<[HOME-04, HOME-05]>`* |
| the deck's shapes put back to ones the sample home holds | `SeededWalkTest` ×3 | *these cards are refused before they can demonstrate anything, against [triangle_up, square, diamond, ring, star, crescent, wide_rect, circle, triangle_down]. Expected `<[]>`, actual `<[SEED001, SEED002]>`* |

### Three injections came back ASLEEP, and two of them became tests

**1. The duplicate-card-id check ignored the terminal.** A file naming one printed id as both a
marker and the terminal read back clean, which is D-069's hazard arriving through the file instead
of through a reprint. `a card id used by a marker and by the terminal is refused` was written, the
injection repeated, and it now fails by name.

**2. The whole scan readout could be deleted from the screen and nothing failed.** The unit is a
card being offered to a map, and the visible half of it is one line in the middle of a black
rectangle — a scan that changed the map and said nothing is a host walking out of a room with the
wrong idea about what is in it. `everyScanOutcomeIsSaidOnTheViewfinder` renders the real screen and
reads all three branches back off it; deleting the readout now fails with *could not find any node
that satisfies: (Text … contains 'KITCHEN . ADDED')*.

**3. `HouseMap.inRoomNamed` is not a guard, and its KDoc said it was.** Replacing it with
`inRoom(Room(name))` breaks nothing, because `RoomKind` has two values (D-098) and a registration
into stairs cannot be constructed (D-099), so every room in a map is ordinary and matches
`Room(name)` exactly. No test was added — there is nothing to catch. The KDoc now says so instead
of claiming a hazard it does not hold off.

### Decisions taken — flagged, not silent

- **The playtest deck's shapes were chosen to miss the sample home's.** They collided at first and
  every ordinary card was refused by D-086 before it could demonstrate anything, on a build whose
  purpose is to walk the flow on a phone. Neither module could have noticed alone, which is why
  the test lives in `app`: `SeededWalkTest` is the only place that can see the deck and the home at
  once.
- **Five of the six scan outcomes are a line on the viewfinder and no movement**; only the terminal
  already being elsewhere is a screen, because that one is a decision. The design's own KEEP
  SCANNING TO ADD MORE is what this is derived from — a host with a stack of paper is not put
  through a screen per card.
- **A refusal always names a shape as well as a room.** The host is holding three cards; THAT SHAPE
  IS ALREADY IN GARAGE with no shape on it names none of them.
- **The card's printed id is shown, quietly, under the readout — the only place in the app that
  ever shows one.** Everywhere else navigates by shape (D-069). A host hunting for one specific
  card among nine has nothing else to go on, and this is host setup in the light.
- **A payload this build cannot read says so plainly** (D-071 permits it) with four messages off
  `CardRejection`. Registration had no screen for a bad card and this is not one — it is a line.
- **The scan readout is cleared when the host opens a different room**, not when they leave the
  scan screen. A readout is about a card *in a room*; stepping out to the marker sheet and back is
  looking, and dropping it there erases the confirmation they went to look at.
- **TAP A MARKER TO REMOVE IT works now**, keyed on the card rather than the shape it draws.
- **The bungalow fixture's cards have readable ids** — `HOME-00` through `HOME-09`, hyphens rather
  than spaces because `MarkerShapes.ALPHABET` is QR alphanumeric **minus SPACE**.
- **`PanelVals.lastRegistered` is deleted.** It was a hardcoded ring; the readout is the editor's.

### Flagged for a person

- **Story 4.5's Array Wipe designations (Spares / Rack / Disposal) are not built.** The unit brief
  named the terminal and not those, and there is no screen for them anywhere in the port. They are
  the other half of 4.5 and they need one.
- **The registration haptic is not stubbed.** D-102's doctrine is that `ScanMarker` buzzes because
  the phone is against a card with the display angled away; that is hardware and this build has
  none, so nothing was faked. A stubbed buzz would be noise nobody could tell from the real thing
  once there is one.
- **`HouseMapStore` now has no caller either.** Unit 4 flagged `HousePlanStore`; the map is stored
  inside `saved-homes.txt` for the same reason the plan is, so `house-map.txt` is a third file
  nothing reads. Three stores for two pieces of data.
- **`Registration` is used for the terminal as well as for markers**, which reads well and means
  the type's name is slightly wider than its KDoc. Worth a look by somebody with an opinion.
- **`PanelState.markersOn` and the `marker*` block of `PanelVals` are still dead** — units 3 and 4
  flagged it; still true, and now that registration is real somebody could decide what 4.4's
  marker-placement mode wants from them.
- **`IntegrityWidget`'s KDoc is still stale after D-103** — units 1, 3 and 4 flagged it.

### Simulator (layout licence only — no BLE, torch, camera or haptics)

Two playtest builds, each with a temporary launch-time effect opening on `ScanMarker` with KITCHEN
held and dealing from the deck, reverted afterwards by targeted reverse-replace.

1. **Three cards.** Two land; the third carries the first's shape and is turned away. Screenshot:
   the bowtie drawn large in the caution ink, THAT SHAPE IS ALREADY IN KITCHEN, `SEED003` under it,
   and THIS ROOM showing the fixture's triangle beside the two that landed. The shutter chip reads
   SCAN SEEDT01 — the next card, named before it is dealt.
2. **Four cards.** The fourth is the card marked T, and the phone is on `TermTaken`: ALREADY
   REGISTERED, *this home has one terminal and it is in* HALL, KEEP IT IN HALL / MOVE THE TERMINAL
   TO KITCHEN, and the footnote about what moving it costs. Every name on it is read off the map.

Whole screen checked on both: status row level with the Island, nothing under the cutout or the
home indicator. The shutter chip overlaps the bottom-left corner of DONE SCANNING, which is the
same cost the PLAYTEST marker has always paid and only cheat builds pay it. Nothing here certifies
anything about the game.


## Unit 6 — the lobby, over the real transport

**Outcome: done, green, pushed.** Commit `8a7634c` — *"Six names become six marks, and the line
goes nowhere but the house"*. Full
`./gradlew check`: **tests=797, failures=0, errors=0** across every module and both targets, up
from **724** at `083f0f6` — this unit adds **73**. Both lints clean (`vocabularyLint clean` on
`model`/`core`/`ui`/`app`, `redactionLint clean — 33 file(s)`), `:ui:boundaryCheck` still reports
`ui clean`, `DeviceLayoutTest` green throughout, and both variant sweeps (`variantSweepRelease`,
`variantSweepPlaytest`) ran.

### What it is

The three cold-start screens were a fixture: a drawn caret where a name should be typed, three
hardcoded networks, a sentence somebody else wrote in the one-line field, and six named seat chips
in a lobby that had never spoken to anything. They are now the real thing — mDNS finds the homes,
a websocket carries a line up and counts down, and **the lobby names nobody**.

- `model/.../InsiderBand.kt` — D-103's balance envelope, written down once. `of(seats)` is the
  band, `clamp(seats, chosen)` is the amendment: **both edges clamp the setting itself**, not just
  the draw, so a host cannot hand-pick a count outside it any more than UNKNOWN can land on one.
  The placeholder edges are the decision's own (`max(1, ⌊seats/6⌋)`..`⌈seats/4⌉`, integer-only —
  a band edge that depends on rounding mode differs between two phones). **Playtest owns the
  numbers, as it owns the 7**; nothing here chose one.
- `model/.../protocol/LobbyWire.kt` — the two bodies that ride inside a `TransportFrame.Carry`,
  and the guard that is the point of the file. `Handover(line)` goes **client → host** only;
  `Standing(joined, linesIn, insiders)` goes **host → client** and has no `String` in it at all.
  One body with a nullable `line` was the obvious shape and is the shape rule 3 forbids — a nulled
  field still exists and somebody makes it non-null later. As two types, *the lobby leaked a name*
  is not a bug that can be introduced by filling in a field.
- `platform/.../transport/LobbyDesk.kt` — the host's table, beside `SeatLedger` and for the same
  reason: pre-arm transport bookkeeping, no `Event`, no `Effect`, nothing that survives arming.
  It holds the lines **in memory with no store reachable from it**, drops a leaver's line with
  them (so `linesIn` can never exceed `joined`, which the gate depends on), clamps the Insider
  setting on every change *and* every time the seat count moves under it, and wipes every line on
  `roundEnded()`.
- `ui/.../Lobby.kt` — `HomeFinder` and `LobbyLink`, which are all `ui` is allowed to know about
  the radio (the same shape `HomeStore` gives it of the filesystem), the `MemoryHomeFinder` /
  `MemoryLobbyLink` fixtures behind every render, `OneLine`, and `LobbyModel` beside `PanelState`
  with the editor and the homes.
- `app/.../LobbyOverTransport.kt` — the one place mDNS and the websocket meet the screens.
  `NearbyHomes` over `HostBrowser`; `LobbyHouse` (ledger + desk + `TransportHost` + advertiser,
  bound on port 0 so the advertisement carries whichever port came up); `LobbyOverTransport`
  joining it as a client. Wired into `CheatRoot`, with discovery events landing in the 0.8 rig's
  log — mDNS failing is otherwise completely silent, which was the entire symptom on the first
  device run.

### The host joins its own house

HOST WITH THIS HOME brings the server up on that phone and then **joins it over loopback like
everybody else**. That is not ceremony: a host reading its own lobby straight out of its own desk
would have counts arriving by a route no other phone uses, and the route no other phone uses is
the one nobody tests. The host's Insider setting goes to the desk sitting next to it and the
answer comes back around the socket — the number the host reads arrived exactly the way the number
everybody else reads did.

### NO NAMES — what was removed, and what replaced it

The lobby drew six seat chips with names in them. They were a fixture, and reinstating them would
be a decision about what a client receives, which is not a call a lobby screen makes. What is
there instead is the same two integers drawn rather than written: one mark per seat, `linesIn` of
them filled. **The fill order is arithmetic, not identity** — this phone was told two integers and
could not order the marks by seat if it wanted to.

The guard is stronger than "the screen happens to have no names to draw", because the phone holds
two pieces of text that would be natural to put there: the resident name its owner typed, and its
owner's one line. `LobbyInputTest` renders the lobby with both set and asserts neither appears.

### The resident name does not leave the phone — a boundary, not an omission

Name entry is real typing and the name stays local. Nothing on the far side reads it: who is in a
round and what the house calls them is the loop's, and the loop is frozen. Handing player-
identifying text up to a host that has no lifecycle for it is a thing to be asked about rather
than started quietly, so it was not started. Wiring it is loop work.

### LIGHTS OUT — the boundary, stated explicitly

**The transition is presentation only.** The button is gated on `everyLineIn` — every seat here,
every line in, read off the house's counts and not off anything this phone decided — and when it
fires it walks *this* phone to the ARMED screen and does nothing else. It does not lock the seat
ledger, draw the Insider count, stamp the balance values into a recording, start a clock, or tell
another phone anything. Every one of those is arming; arming is the loop's; the loop is frozen. In
play this button becomes an intent the house answers by pushing a screen to everybody at once,
which is exactly why none of it may be decided on a device. `FlowModel.lightsOut`'s KDoc says the
same thing at the call site, where somebody will actually read it.

The gate is also the host's alone. A client is told *WAITING FOR THE HOST* rather than given a
button that would do nothing — the host turns the lights off, and everything after that is the
house answering rather than the host announcing.

### Guards, and the bug injected into each

Every one was proved by injecting the fault it exists for, watching it fail, and reverting with a
targeted reverse-replace.

| Injected | Caught by | Failure, from the XML |
|---|---|---|
| `LobbyDesk.handedOver` also calls `saveSavedHomes(line)` | `OneLineNeverPersistedTest` (iosTest, real Documents directory) | `tests="1" failures="1"` — *"a one line was written to saved-homes.txt"* |
| `LobbyDesk.setInsiders` stores `chosen` instead of `InsiderBand.clamp(...)` | `LobbyDeskTest` | `tests="11" failures="1"` — *"the maximum edge did not clamp the setting. Expected <2>, actual <5>"* |
| The lobby draws `lobby.residentName` above the presence strip | `LobbyInputTest` | `tests="10" failures="1"` — *"the lobby printed the name typed on this phone expected:<0> but was:<1>"* |
| `OneLine.toString()` interpolates the text | `LobbyModelTest` | `tests="13" failures="1"` — *"the one line printed itself: OneLine(i still have priya's spare key, handedOver=false)"* |

**The never-persisted sweep is self-verifying.** `OneLineNeverPersistedTest` runs on the simulator
against the *real* Documents directory — where every durable store this app has writes, so it is a
sweep of everything the phone can keep — and it first writes a canary through `saveSavedHomes` and
asserts the sweep **finds** it. A test that reads no files passes for the same reason a test that
reads all of them does; the positive control is what makes the negative result mean anything. It
also checks fragments, not just whole lines: the first four words of somebody's line identify it
completely in a room of six people who all know each other.

`LobbyLoopbackTest` runs the whole exchange over an actual socket — one host, two clients on
loopback, lines up, standings down, a phone leaving and taking its line with it — which is the
code the two-phone evening will run. `HostDiscoveryTest` (already green) covers advertise-and-
resolve on the simulator. **No phone was required for any of it.**

### Two shared controls stopped lying

Both were found by a test that needed the truth and are worth knowing about beyond this unit:

- **`RowButton`'s `onClick` defaulted to an empty lambda**, so every settings row in the game
  published a click action that did nothing. It is nullable now, and a row with no handler is not
  a tap target. This matters most on the lobby, where the same settings are the host's to change
  and everybody else's to read, and where the difference has to be visible in what the screen does
  when you press it.
- **`PanelButton` has the same problem** and was *not* changed — twenty-five call sites, and none
  of them are load-bearing on it today. The gated LIGHTS OUT therefore draws its own inert block
  rather than reusing `PanelButton`, with the reason at the call site. Flagged below.

### Decisions taken inside the design language, and flagged

1. **Signal bars are gone from the Join rows; the address is in their place.** mDNS resolves a
   name, an address and a port and reports no signal strength at all, so `///` would be an
   invention on the screen a player uses to pick which house they are walking into. The address is
   also the only true thing that separates two homes when both households have called theirs THE
   BUNGALOW — which is why the nearby list dedupes by address and port and never by name.
2. **`ReadoutField` grew a `hint`.** The design drew a blinking `Caret` in every field to promise
   the device was listening; a real field shows its caret only once focused, so an empty one reads
   as a box rather than as somewhere to type. YOUR NAME and `one line` are the two places it is
   used — both screens where somebody is being asked for something.
3. **The one-line field does not shout.** `transform` is identity there, against the field's usual
   uppercase: a house name is a label and a confession is a sentence.
4. **A blank line is refused and the screen stays put**, with the reason under the button — the
   same shape as a refused SAVE HOME, and for the same reason: walking away would leave the player
   believing the house holds something it does not, with the lobby's count agreeing with them.
5. **`0 OF 0 HANDED THEIRS OVER` is not drawn** while nobody has joined; the strip above already
   says NOBODY HAS JOINED YET in words.

### The screen graph moved, and the two new edges are the actions layer's

`ScreenGraph.exitsOf(Secret)` is now empty and `exitsOf(Lobby)` is `{Secret}`; `Flow.viaActions`
gains `Secret → {Lobby}` and `Lobby → {Armed}`. Both belong there under the rule already written
in that file — where a tap lands depends on an answer the screen does not have. HAND IT OVER can
be refused; LIGHTS OUT is a control only while the house's counts say the gate is closed, so
whether the screen publishes it at all is not a fact about the screen. `Flow.unrouted` is still
empty and `FlowTest` walks both new edges.

### Looked at

`ScreenshotTest` gained a second viewer for the lobby's three states the id-driven sweep
structurally cannot reach — **empty** (what a host sees for the first thirty seconds of every
evening), **client** (no settings control, no LIGHTS OUT), and **ready** (the gate open, the slate
button live). All three were rendered and read whole, along with Join and Secret. `DeviceLayoutTest`
renders all 56 screens × 2 roles against a faked notch and stayed green.


## Unit 7 — notifications: one gesture, and what survives it

**Outcome: done, green, pushed.** Commit `PENDING` — *"Everything that pops, and the one gesture
that takes it away"*. Full `./gradlew check`: **tests=814, failures=0, errors=0** across every
module and both targets, up from 797 — 17 new (`NotificationInputTest` 8, `NotificationsTest` 4 and
one `ScreenshotTest` viewer on desktop, `FlowTest` +2 on both desktop and `iosSimulatorArm64`).
Both lints clean,
`:ui:boundaryCheck` still reports `ui clean`, and `DeviceLayoutTest` stayed green throughout.

### What it is

D-105 deletes the read concept and replaces the whole surface with one idea: *everything that pops
is a notification, swipe up dismisses it, and that is the whole gesture vocabulary.* The port had
banners; it had no gesture, and the interesting half of D-105 — what is still there afterwards —
was not expressed anywhere.

- **`ui/.../Notifications.kt`** — `NotificationKind`, `Notification`, and the three the port draws.
  The load-bearing field is `NotificationKind.heldBy`: **where the thing survives once the banner
  is gone, or `null` for nowhere.** Messages holds every text, the Egress widget holds the
  countdown, and a house notice is held nowhere at all. It is a field rather than a paragraph
  because a paragraph cannot be tested and this is.
- **`NotificationBanner`** replaces `HouseBanner` and `EgressBanner` — one composable taking one
  piece of data, so there is no per-kind banner to write differently by accident. It tracks the
  finger the whole way up, springs back short of `SWIPE_DISMISS`, and only ever moves **up**: down
  is not a shorter swipe up, it is a second gesture, and this app has one.
- **`PanelFrame`'s overlay box now clips.** The banner moves, and the status row is the one thing
  on screen that must not — it is how a player confirms the perimeter is still armed while
  something is arriving. The banner slides *under* the boundary rather than over the clock.
- **`FlowModel.dismissNotification`** — the actions-layer edge, declared in `Flow.viaActions`
  beside HOLD TO DELETE and the other gestures no synthetic click can reach, and walked by
  `FlowTest`. **Nothing is written down**: not which notification it was, not that it was
  dismissed, not when.

### Three sentences that were each written twice are now written once

Every notification appears on two surfaces — the banner, and the thing that holds it afterwards —
and two copies of a sentence agree on the day they are written and never again.

| said twice | now |
|---|---|
| the house's text, on the banner and as the Messages preview | `Notifications.text.body`, and `NotificationKind.Text.heldBy` says why |
| UTILITY and LANDING, on the Egress alert and on the widget | `Notifications.EGRESS_NODES`, joined two ways |
| the house notice, on the meeting screen | `Notifications.notice.body`, drawn in exactly one place |

The Egress pair is the one that would have hurt. Containment needs two people at two separate
markers and **nobody may speak**, so the pair of room names *is* the coordination; a widget naming
a different pair from the alert would send two silent people to opposite ends of a dark house, and
it would look like a typo in the diff.

### "Stored nowhere" is proved by looking everywhere else

There is no notifications model, and that is the strongest form of the property — but it is also
unprovable by inspection, because the way this feature grows a memory is that somebody adds an
inbox, a history, a list-of-what-arrived, and the notice is caught on its way past. So
`NotificationsTest` renders **all 56 screens in both roles** and asks which ones have the notice's
words on them. The answer must be exactly `{Notice}`. A leak into Messages, Notes, a thread or a
tile fails there and is named.

Its converse is in the same file, because a rule that only ever says *fewer screens* is satisfied
by an app with no screens on it: the text banner **must** still be readable in Messages, and the
Egress nodes **must** be named on both the alert and the widget.

### D-105's other half: no read state, held by rendering rather than by lint

`noSurfaceCountsWhatHasBeenLookedAt` renders every screen in both roles and fails on UNREAD,
UNOPENED, UNSEEN, or NEW as a tag. The one count the design keeps is the backlog header — *N
MESSAGES* — which counts what is there rather than what has not been attended to.

**A vocabulary-lint stem was considered and rejected.** `VocabularyRules` matches stems by
containment, and `unread` is a substring of `unreadable`, which is `SavedHomesModel`'s own word for
a store it could not read. A stem there would have fired on four existing call sites and been
switched off within a week; and the words that would actually need catching (`isRead`, `readAt`)
cannot be matched by containment without also catching `thread`, `already`, `Reader` and
`spread`. Rendering catches the thing that matters — a mark a player can see — which the lint
never could.

### Injection log — eight bugs written, watched to fail, reverse-replaced

Each was reverted with a targeted reverse-replace, never `git checkout`.

| injected | caught by | XML |
|---|---|---|
| the swipe threshold deleted — any drag dismisses | `NotificationInputTest`, two tests | `tests="7" failures="2"` — *"a short drag dismissed it anyway expected:<Notify> but was:<Home>"*, *"dragging the banner DOWN dismissed it"* |
| the notice added to the Messages backlog | `NotificationsTest` | `tests="4" failures="1"` — *"a house notice is shown once and stored nowhere — these screens still have it expected:<[Notice]> but was:<[Reveal, Notice]>"* |
| a NEW tag on every inbox row | `NotificationsTest` | `tests="4" failures="2"` — *"Did not expect any node but found '7' nodes that satisfy: Text contains 'NEW'"* |
| the Egress widget naming STUDY instead of LANDING | `NotificationsTest` | (same run) *"LANDING is named on [Banner] — the alert and the widget it survives into must agree"* |
| `dismissNotification` navigating with no banner up | `FlowTest` | `tests="30" failures="1"` — *"Boot has no banner and a swipe moved the phone expected:<Boot> but was:<Home>"* |
| `clipToBounds` removed from the overlay box | `NotificationInputTest` | `tests="7" failures="1"` — *"the banner reached into the status band while being swiped away — first at (12,46)… The bar is the one thing that stays put."* |
| the panel dimmed unconditionally, not with the banner | `NotificationInputTest` | `tests="7" failures="1"` — *"the panel behind the banner was at 2.874 and came back to 2.874 — the dim did not go with the banner"* |
| the banner's tap retargeted from Messages to Notes | `NotificationInputTest` **and** `FlowTest` | `tests="8" failures="1"` — *"a tap on the house's text did not open Messages expected:<Reveal> but was:<Notes>"*; `tests="30" failures="1"` — *"the Text notification opens Notes and survives into Reveal"* |

**Three of these changed a test, and two of them were my own guards failing.**

1. **The clip test passed with the clip deleted.** It swiped the banner so far that it had left the
   status band entirely by the time the frame was captured — a check that measured a banner which
   was not there. It now swipes a bounded distance *and asserts, from `positionInRoot`, that the
   banner really is over the band before measuring anything*, so a future version that overshoots
   fails loudly instead of passing quietly.
2. **The tap test asserted a value equalled itself.** It read the expected screen back off
   `Notification.opens` — the very field being injected — so the retarget went through green. It
   names `ScreenId.Reveal` outright now, and `FlowTest` gained the invariant behind it: **where a
   notification opens is where it survives** (`opens == kind.heldBy`), which is why two independent
   tests catch it on the second run. Worth noting that `ScreenGraphTest` structurally cannot: every
   springboard screen is already an exit of `Notify`, because `Notify` *is* the springboard with
   something on top.
3. **A claim in a KDoc turned out to be false.** The banner's doc said the drag detector sits
   outside the tap "which is why" both gestures work. Swapping the two modifiers and re-running all
   seven gesture tests showed it makes no difference — a drag consumes nothing until touch slop,
   and a tap past slop is no longer a tap. The comment now says that, and says it was checked.

### Decisions taken (minor, inside the established design language) — flagged, not silent

- **A swipe on a screen with no banner does nothing**, rather than navigating anyway. Both banner
  screens draw the *same* springboard underneath, so a dismissal that fired regardless would send
  a player who flicked at a tile back to page 1 from wherever they were — and it would be blamed
  on the touchscreen. `FlowTest` walks every screen in the game to hold it.
- **`SWIPE_DISMISS` is 14 design units.** Longer than a brush, shorter than a throw. Both failure
  directions are real in a dark room with one thumb: a dismiss that fires on contact throws away
  the one thing the house said, and a dismiss that needs a full swipe is a player fighting their
  phone. It is a presentation number, not a balance value.
- **The gesture tests read touch slop and the threshold out of the running composition** rather
  than hardcoding pixels. A hardcoded distance is a test that keeps passing while silently no
  longer exercising the branch it names.
- **`ScreenshotTest` gained a third viewer** — a banner at rest, mid-swipe and going, against a
  faked notch. The travel is a position, and a position is not a `ScreenId`, so the id-driven
  sweep structurally cannot reach it.

### Flagged for a person — not escalations, but somebody should look

- **The house notice still has a DISMISS button, and D-105 says the vocabulary is one gesture.**
  The port draws house notices as a full meeting screen (`ScreenId.Notice`, in the
  Assemble→Notice→Discussion line) with a button on it. D-105 calls house notices *"the popup from
  a phantom app"* and says swipe-up is the whole gesture vocabulary — which reads as though the
  notice should be a banner like the other two. Turning it into one restructures the meeting line,
  which is not a notifications change, so **the kind exists and its words are single-sourced, and
  the surface was left exactly as drawn.** Somebody should decide whether the notice is a screen
  the meeting stops at or a banner over the meeting.
- **The Perms screen lists a NOTIFICATIONS permission as PENDING.** That is the *operating
  system's* notification permission, and nothing in this unit is an OS notification — the phantom
  OS draws its own. Whether the real app ever asks for the system one (for a banner while
  backgrounded, say) is a question nobody has answered, and it is sitting on a screen telling every
  player it will be asked for.
- **`Notification.bodySize` is presentation on a data class.** It earns its place today because a
  headline and a sentence are set differently, but a data type carrying a font size is the sort of
  thing that grows a second one. Worth a look if a fourth kind appears.

### Looked at

Three banner frames rendered to PNG and read whole (`ui/build/screens/banner-*.png`): at rest the
panel behind is at `NOTIFIED_DIM` with the status row bright and clear above it; mid-swipe the
HOUSE/NOW header has gone under the boundary with only the body line showing and **the status row
untouched**; the amber block ends cleanly on the band edge rather than riding over it.

Playtest variant built, installed and launched on the simulator. It opened on `Boot` and fell
through to `Perms` on its own, as before. Whole screen checked: status row level with the Island,
nothing under the cutout or the home indicator, the PLAYTEST chip in place, no errors in the device
log. **It says nothing about this unit's work** — reaching a banner needs taps the simulator path
cannot make — so the visual instrument here was the render harness above plus `DeviceLayoutTest`,
which renders all 56 screens × 2 roles against a faked notch and stayed green.


## Escalations

Entries here were stopped rather than guessed. Each names enough for a morning ruling.

### E7-1 · A swipe-up returns the panel from `NOTIFIED_DIM` to full — is undimming a client's to do? NOT DECIDED, gesture built

**Raised by:** Unit 7, building D-105's dismissal.

`NOTIFIED_DIM` is not styling and `Chrome.kt` already says so: a phone held as a lamp faces away
from its owner, so the drop in emitted light **is** the notification for anyone who cannot read
their own screen. It is world-observable, which is why the same note requires every banner to go to
everybody at once — a banner addressed to fewer than all players would be a beacon pointing at
whoever got it. And project rule 5 is that light is game state: the dim has to arrive as an
authored effect from the rules, as a step and never a fade, because *a ramp nobody authored is a
signal nobody authored*.

**The dismissal is the other end of that, and it is now a client-side gesture.** Before this unit
the port dimmed on `Notify` and not on `Home` and nothing moved between them; now a player's own
thumb does. Six phones dismiss at six different moments, so six lamps come back up at six different
moments, and that is a light change in a dark room that the house did not author.

**Why it was built rather than skipped.** The gesture is the unit, and there is no version of it
that leaves the light alone: a banner that stayed dimmed after being swiped away is equally a lamp
decision and a worse one. What is genuinely undecided is the mechanism, not the feature.

**What needs ruling:**

1. **Is dismissal local, or an intent the house answers?** D-097 already settled the shape of a
   refusal — the authority re-asserts the client's authorized view — and dismissal could ride the
   same mechanism: the phone asks, the house pushes the undimmed screen. That is the consistent
   answer and it costs a round trip on a gesture that must feel instant in the dark.
2. **If it is local, is the staggered undim acceptable?** The argument that it is: everybody got
   the same banner, so a phone brightening says only *"that player dismissed theirs"*, which is not
   alignment information and is not something an Insider can produce and a Resident cannot.
3. **Does the undim need to be a step rather than a fade** in the same way the dim does? It is a
   step today, because it is a screen change and nothing animates it. Nobody has said whether that
   is required or merely true.

**Nothing was assumed.** `NOTIFIED_DIM` itself is untouched — the dim is still exactly the
existing rule, still tied to the presence of an overlay, and `NotificationInputTest` now measures
that tie in pixels rather than trusting the flag.

### E6-1 · Does the resident name leave the phone, and does any client ever receive another one — NOT DECIDED, skipped

**Raised by:** Unit 6, wiring the Join screen's name field.

The name field is real typing now and **the name stays on the device**. Nothing sends it anywhere,
because nothing on the far side reads it: who is in a round and what the house calls them is the
loop's, and the loop is frozen. Handing player-identifying text up to a host that has no lifecycle
for it — no place to keep it, no rule for when it is dropped — is how a promise gets made by
accident.

But the question behind it is not a wiring question and cannot be answered by a unit:

1. **Does the host learn names at all?** It plausibly must — a meeting has to be able to say who is
   being restrained, and the app is everybody's only light.
2. **If it does, does any client ever receive one?** That is a what-a-client-receives call. The
   design's lobby shows *counts*, and this unit held that line: `LobbyBody.Standing` is three
   integers and physically cannot name anybody. Widening it later means adding a field in a diff
   that says so, which is the property the two-type split was built for.
3. **If clients do receive names, when?** A name list in the lobby and a name list at a meeting are
   different disclosures. Occupancy is not public in this game — it is what the meeting is *for*.

**What is in the tree meanwhile:** `LobbyModel.residentName`, local, bounded, drawn only on the
screen it is typed on, and asserted absent from the lobby by `LobbyInputTest`. Nothing has to be
undone whichever way this goes; something has to be added.

### Unit 6's other client-boundary calls were the unit's own authorisation, recorded rather than raised

Three things cross to a client in this unit, and none of them was a judgement call made here:

- **The joined count and the handed-over count.** The design's lobby shows both — *6 JOINED*,
  *4 OF 6 HANDED THEIRS OVER* — and the unit brief names them explicitly. They are counts of a
  social fact that six people standing in a lit hall can already see.
- **The Insider setting, including UNKNOWN.** D-103: *the settings row may read UNKNOWN*. A chosen
  number is public because the host chose it in front of everybody; UNKNOWN is public because it is
  the host saying they are not choosing.
- **Nothing else.** In particular no seat identity, no name, and no line — and the type that goes
  down cannot carry any of them.

**Also worth a person's eye, not a ruling:**

- **`PanelButton` publishes a click action even with no handler** (its `onClick` defaults to an
  empty lambda), the same fault `RowButton` had and that this unit fixed there. Twenty-five call
  sites, none load-bearing on it today, so it was left alone and the gated LIGHTS OUT draws its own
  inert block instead. A control that answers a press by doing nothing is indistinguishable from
  one that is broken, which is worth a sweep on a morning when nothing else is on fire.
- **The lobby is only reachable in playtest and debug.** The release variant root still draws the
  boot screen and nothing else (pre-existing, and honest about it in its own KDoc) — so the real
  transport wiring compiles into release and is constructed by nobody. That is fine while the loop
  is frozen and is the first thing that has to change when it is not.

### E5-1 · What payload marks the card as the Terminal — DECIDED PROVISIONALLY, needs ratifying

**Raised by:** Unit 5, building `TermTaken`, `TermRemove` and the `NoTerminal` gate.

The host-setup screens have always said *scan the card marked T* and *the T card is never an
ordinary marker*, and nothing in the design says how a scan tells one. A card's payload carries a
version, a shape and an id (D-069) and nothing else, so **the shape is the only field that can say
what kind of card this is**.

**What was decided, because the unit could not be built without it:** `t_shape` — literally the
letter T, which is what is printed on the card — is reserved. `MarkerShapes.TERMINAL` names it,
`MarkerShapes.registrable` is the other 43, `MarkerCard.isTerminal` reads it off the payload, and
`HouseMap` refuses it as an ordinary registration and refuses anything else as the terminal. The
roster still holds all 44 entries, because it is wire data and ids are never renumbered — this is
a shape that is spoken for, not one that was removed.

**Why it needs a ruling anyway.** It decides what is printed on paper and paper cannot be patched:
every card the QR sheet (4.11) ever prints is downstream of it. The alternatives not taken were a
reserved *id* prefix (invisible to a host holding the card, and the id is meant to be opaque) and
a reserved *version* (`CardPayload.decode` already refuses any version but 1, so it would mean
teaching the decoder that some refusals are not refusals).

**What needs ruling:** whether `t_shape` is the reservation, or whether the T card is marked some
other way — and, if the answer is yes, whether the three Array Wipe stations (Spares, Rack,
Disposal, the rest of story 4.5) are reserved the same way, which would take the ordinary roster
from 44 to 40 and is a decision about the shape set the legibility pass produced.


### E1-1 · The restrained player's takeover at the halfway mark of the vote results

**Raised by:** Unit 1, transcribing `Tally`'s LIGHTS OUT IN countdown.

D-102 (revision 20) ratifies: *"The restrained player's takeover appears at the halfway mark of
the vote results so they do not walk away when the countdown ends."* So `Tally` has **two**
outgoing transitions on the same countdown — everybody goes to the springboard when it ends, and
one player goes to `Restrained` halfway through it.

`Flow.autoAdvance` transcribes only the first. The second cannot be a client-side rule: which
player it fires for is a game answer, and a table in `:ui` that could express *"…and if it was
you, this other screen"* would be the device deciding something the house decides. `Restrained` is
listed in `Flow.housePushed` accordingly, and a test asserts nothing walks to it.

**What needs ruling:** whether the halfway takeover is a second authority-side push (the house
sends `Restrained` to one seat at T+half while the others sit on `Tally`), which is what the
architecture implies and what I assumed — or whether the design intends something the client
schedules. If it is the former, it is loop work and the freeze covers it; the note here is only so
that nobody later reads the missing row as an omission. **It touches what a client receives**,
which is why it was not decided by an overnight unit.

### E1-2 · What a `Revoked` phone does when a meeting is called

**Raised by:** Unit 1, while establishing that `Revoked` has no way onward.

The ported sequencing (`ScreensOut.kt`'s header, and D-075's timing argument) is: a revoked player
sits in the dark with a dead device; they stand and walk in when a meeting is called; only after
that meeting ends does the outside view appear. `Revoked → Ghost2` is therefore real, and it is
triggered by *a meeting being called* — a game event, not a countdown. So it is a house push and
was left out of the auto-advance table, with `Revoked` sitting in the "waiting for the house" set.

**What needs ruling:** nothing, if that is right — but it means a revoked phone in the current
build sits on `Revoked` forever with no control on it, which is correct behaviour that looks
exactly like a hang. The same applies to `Restrained`, `Ghost3`, `Disconnect` and the two endings.
Worth a deliberate confirmation that "a screen with nothing on it, waiting" is the intended
resting state for a player who is out, rather than something the design expects to move.

### E2-1 · The GDD and the epics still describe three room types; D-098 deleted the third

**Raised by:** Unit 2, deciding what `RoomKind` tokens `HousePlanText` may read.

D-098 (revision 20) ratified that the third room type is **deleted, everywhere** — the Insider's
route between rooms is Override, and Override must never be drawable on a map. `RoomKind` in
`model` has held exactly `Room` and `Stairs` since then, and `HousePlanText` reads those two
tokens and refuses any other, naming D-098 in the refusal. That part is settled and shipped.

**What is not settled is the planning artifacts, which still describe the deleted type as live:**

- `gdd.md:783` — the grid painter paragraph offers a shape three tags, not two.
- `gdd.md:799` (the marker taxonomy table) — two placement rules are stated in terms of it.
- `epics.md:150` (story 4.2) — *"Tag a shape as room / … / stairs"*.
- `epics.md:150` (story 4.7) — an editor exclusion written entirely about it, including *"a
  Terminal behind an Insider-only door is unrecoverable"*.

**Why this was not fixed by an overnight unit.** Story 4.7's exclusion is not merely stale
wording: it is a real hazard statement, and if the concern survives D-098 it now has to be
expressed some other way — the design deleted the *map type*, not the fact that some interior
doors are Insider-only. Deciding whether 4.7 becomes an Override-side rule, moves to the
adjacency model, or is deleted with the type is a design ruling, and rewriting a GDD paragraph and
two epic rows to guess at it is exactly the quiet, permanent kind of wrong this project is
organised around refusing.

**What needs ruling:** whether 4.7's hazard has a home after D-098, and — once that is answered —
a sweep of the four sites above so the planning artifacts stop describing a room type the code
refuses to read. Story 4.6 needs nothing: *no marker in a stairs zone* is already structural
(D-099, `Registration`'s `init`).

*(The word itself is not written here, in code, or anywhere else in the repo — CLAUDE.md's
vocabulary is closed and no longer carries it. The line references above are enough to find each
site.)*

### Unit 4 raised none either, for the same structural reason

Saved homes are host-side setup data: named, painted and stored in the light, weeks before
anybody plays, and **never read during a round**. Nothing here receives anything at a client
boundary, reveals nothing about anybody's alignment, never touches the lamp, and adds no `Effect`
and therefore no schema entry. The two rules it does enforce — stairs hold nothing (D-099) and one
home, one terminal — were already ruled on and were applied rather than re-derived. The loop is
untouched: no Events, Effects, redaction-schema rows or `reduce()` changes.

The two things worth a person's attention are in *Flagged for a person* above rather than here,
because neither is a ruling: the unused `HousePlanStore`, and the fact that a hand-painted home
cannot be saved until story 4.5 gives the terminal a way onto the map.

### Unit 3 raised none, and that is a fact about the unit rather than luck

The home editor touches nothing the escalation rule covers. It reads and writes a `HousePlan`,
which is host-side setup data: drawn in the light, weeks before anybody plays, saved to the
device, and **never read during a round**. It receives nothing at a client boundary, reveals
nothing about anybody's alignment, never touches the lamp, and adds no `Effect` and therefore no
schema entry. The two places it could have drifted into a ruling — what a room becoming stairs
does to the cards in it, and whether the map may carry a third room type — were both already
ruled on (D-099, D-098) and were surfaced rather than re-derived.
